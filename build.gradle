allprojects {
    apply plugin: 'java'
    repositories {
        mavenCentral()

        // Add OSS Sonatype Snapshot repository
        maven {
            url 'https://oss.sonatype.org/content/repositories/snapshots'
        }
    }
}
